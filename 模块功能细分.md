# 声印项目模块功能文档

## **基础层 (Basic Layer)**

### **模块1：用户账户与认证（User & Auth）**

**功能描述：**

1. **用户注册**

   * 支持邮箱/手机号注册。
   * 校验输入合法性。
2. **用户登录**

   * 支持账号密码登录、第三方授权登录(可扩展)。
   * 登录状态保持和会话管理。
3. **退出登录**

   * 清理本地登录状态，返回未登录界面。
4. **密码重置/找回**

   * 支持通过邮箱/手机号找回密码。
5. **基础信息管理**

   * 用户昵称、头像修改。
   * 信息实时更新。

**依赖关系：**

* 所有内容上传、评论、收藏、点赞、检索模块都依赖登录状态。

---

## **声音内容层 (Content Creation Layer)**

### **模块2：声音采集与上传（Audio Capture & Upload）**

**功能描述：**

1. **定位获取**
   * 必须先获取用户当前位置，确保声音与地理位置高度绑定。
2. **录音功能**

   * 移动端录音，最长60秒（或者其他时间）。
   * 录音实时波形显示，支持暂停、取消、重录。
3. **上传声音**

   * 录音完成后上传至云端，并与地理坐标绑定。
   * 支持隐私设置（公开 / 仅自己可见）。
4. **描述与标签添加**

   * 用户可手动输入描述或标签。
   * **LLM智能建议**：根据录音内容和位置提供录音描述或标签推荐，用户可采纳或修改。

**依赖关系：**

* 依赖模块1：用户必须登录。
* 依赖模块3：录音上传后存储在声音记录管理中。
* 依赖模块4：录音与地图坐标绑定。
* 依赖模块5：可为录音添加标签。

---

### **模块3：声音记录管理（Sound Record Management）**

**功能描述：**

1. **声音浏览与管理**

   * 用户查看自己历史上传声音。
   * 支持删除或修改描述/标签，但**不可修改已绑定的定位信息**。
2. **隐私控制**

   * 修改单条声音的公开权限。
3. **音频回放**

   * 支持播放、暂停、进度控制。
4. **标签管理**

   * 与模块5联合使用，用户可管理声音标签。

**依赖关系：**

* 依赖模块1：用户登录状态。
* 依赖模块2：声音上传生成记录。
* 依赖模块4：地图显示声音点。

---

### **模块4：位置与地图（Location & Map）**

**功能描述：**

1. **地图显示与浏览**

   * 支持拖动、缩放、定位当前位置。
2. **声音标记点展示**

   * 显示用户上传或公开声音的地理位置。
   * 不同密度区域可聚合显示。
3. **搜索地点**

   * 可通过关键词搜索，移动地图到目标位置。
   * 可点击地图上的位置反向获取地名。
4. **地理位置绑定**

   * 声音上传前获取定位信息。

**依赖关系：**

* 依赖模块2：上传声音必须绑定坐标。
* 依赖模块3：声音记录信息用于地图展示。
* 依赖模块5：标签可作为地图筛选条件。
* 依赖模块10：检索与筛选可联动地图显示。

---

### **模块5：标签系统（Tag System）**

**功能描述：**

1. **标签创建与推荐**

   * 用户可创建自定义标签。
   * 上传声音时提供推荐标签。
2. **标签管理**

   * 编辑、删除、查看所有标签。
3. **标签筛选**

   * 可通过标签筛选声音记录或地图标记点。

**依赖关系：**

* 依赖模块2：声音上传时可添加标签。
* 依赖模块3：声音记录可管理标签。
* 依赖模块4：地图显示可筛选标签。
* 依赖模块10：检索与筛选模块使用标签。

---

## **互动层 (Interaction Layer)**

### **模块6：评论与回复（Comment & Reply）**

**功能描述：**

1. **评论发布**

   * 用户可对公开声音发布文字评论。
2. **评论管理**

   * 用户可删除自己评论。
   * 回复他人评论。
3. **实时更新**

   * 评论和回复在页面实时显示。

**依赖关系：**

* 依赖模块1：用户登录。
* 依赖模块3：声音记录管理提供评论目标。
* 依赖模块9：首页推荐或检索模块展示内容。

---

### **模块7：点赞（Like）**

**功能描述：**

1. **点赞/取消点赞**

   * 支持单条声音记录点赞，实时更新计数。
   * 支持给声音下的评论点赞，每次打开评论区更新计数
2. **互动统计**

   * 显示点赞总数。

**依赖关系：**

* 依赖模块1：用户登录。
* 依赖模块3：声音记录。
* 依赖模块6：用户评论。
* 依赖模块9：首页推荐或检索模块。

---

### **模块8：收藏（Favorite）**

**功能描述：**

1. **收藏/取消收藏**

   * 用户可收藏喜欢的声音。
2. **收藏管理**

   * 用户查看个人收藏列表。
   * 用户可管理个人收藏列表，设置置顶、删除收藏的声音。
   * 用户可点击收藏的声音跳转到地图上该声音对应的位置，并且查看评论区。

**依赖关系：**

* 依赖模块1：用户登录。
* 依赖模块3：声音记录。
* 依赖模块9：首页推荐或检索模块。

---

## **分发与访问层 (Access Layer)**

### **模块9：首页推荐（Feed & Discovery）**

**功能描述：**

1. **内容推荐**

   * 根据用户兴趣、历史行为推荐声音内容。
2. **地图与内容联动**

   * 点击推荐内容跳转地图对应位置。
3. **展示用户交互数据**

   * 显示点赞、评论数量。

**依赖关系：**

* 依赖模块3：声音记录数据。
* 依赖模块4：地图显示位置。
* 依赖模块6/7/8：互动信息。

---

### **模块10：检索与筛选（Search & Filter）**

**功能描述：**

1. **搜索功能**

   * 支持关键词搜索地名、声音描述、标签。
2. **筛选功能**

   * 按标签、上传时间、用户等筛选声音。
3. **搜索结果联动地图**

   * 搜索后地图移动到目标区域，显示相关标记点。

**依赖关系：**

* 依赖模块3：声音记录信息。
* 依赖模块4：地图联动显示。
* 依赖模块5：标签筛选。

---

## **附加功能层 (Optional Layer)**

### **模块11：通知与消息（Notification & Messaging）**

**功能描述：**

1. **用户消息推送**

   * 评论、点赞、收藏提醒。
2. **系统通知**

   * 系统更新、功能提示。
3. **消息管理**

   * 用户可查看消息历史、标记已读。

**依赖关系：**

* 依赖模块1：用户登录。
* 依赖模块6/7/8：互动事件触发通知。
* 依赖模块9/10：内容推荐或检索变化可触发通知。

---

## **LLM（仅模块2集成）**

**作用说明：**

* **提供录音建议**：提醒用户可录制的环境声音或场景声音。
* **标签/描述推荐**：生成标签和文字描述建议，用户可采纳或修改。
* **不替代用户操作**，仅作为参考辅助。

