@startuml UC11_NearbyRecords
|Frontend|
start
:进入地图界面;

|Frontend|
if (用户授权GPS?) then (yes)
  :获取当前 GPS 坐标;
  if (获取成功?) then (yes)
    |Audio|
    :查询半径范围内的记录;
    if (附近有记录?) then (yes)
      |Frontend|
      :在地图上展示记录点;
      |Frontend|
      :点击某个点查看详情;
      |Audio|
      if (记录位置有效?) then (yes)
        |Frontend|
        :显示记录详情;
      else (位置数据损坏)
        |Frontend|
        :提示“记录位置不可用”;
      endif
    else (附近无记录)
      |Frontend|
      :显示空列表（地图无记录点）;
    endif
  else (获取失败)
    :提示“定位失败，GPS信号弱”;
  endif
else (未授权)
  :无法加载“附近”模块，提示授权GPS;
endif

stop
@enduml
