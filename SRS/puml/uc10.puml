@startuml UC10_BrowseHotRecommendations
|Frontend|
start
:打开首页“热门推荐”模块;

|Audio|
if (服务可用?) then (yes)
  :获取排序后的热门列表;
  |Audio|
  :过滤已删除或不可访问记录;
  |Frontend|
  :展示热门记录列表;
else (服务不可用)
  |Frontend|
  :显示空列表，并提示“服务不可用”;
endif

|Frontend|
if (网络正常?) then (yes)
  :显示热门记录;
else (网络异常)
  :提示“加载失败，请检查网络”;
endif

stop
@enduml
