@startuml
|SSE|
start
:检测到有待发送的通知;
:用户侧显示有未读通知;

|Frontend|
:点击通知图标;
:进入通知中心;
|Notification|
if (通知内容异常?) then (yes)
  |Frontend|
  :显示“通知内容暂不可用”;
  stop
else(no)
   :查看通知内容;
endif
if (通知存在?) then (yes)
   :获取通知;
   |Frontend|
  :显示通知列表;
else (no)
  |Frontend|
  :提示“没有通知”;
  stop
endif


|Frontend|
if (选择批量处理通知?) then (yes)
  |Notification|
  if (选择一键清空所有已读通知?) then (yes)
    :清空所有已读通知;
  else (no)
    :批量标记通知为已读;
  endif
else (no)
  if (选择通知静默?) then (yes)
    :设置通知静默，红点不再提示;
  else (no)
    :继续其他操作;
  endif
endif


|Frontend|
:选择某条通知查看详情;
|Notification|
:将该通知标记为已读状态;
|Audio|
if (加载音频详情内容成功?) then (yes)
  :获取音频详情内容;
  
  |Frontend|
  :显示音频详情内容及相关操作选项;;
else (no)
  |Frontend|
  :提示"音频加载失败，请刷新页面重试";
  stop
endif

if (网络连接中断?) then (yes)
  :提示“网络连接异常，通知同步暂停”;
  stop
endif



stop
@enduml