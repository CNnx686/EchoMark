@startuml UC15_ManageFavoriteAudios
|Frontend|
start
:进入“我的收藏”页面;

|User|
if (获取个人收藏列表成功?) then (yes)
  :加载用户所有收藏记录;
  :按收藏时间排序;

  |Audio|
  if (音频存在且可用?) then (yes)
    |Frontend|
    :显示收藏列表;
    :选择操作（取消收藏 / 查看详情 / 地图定位）;

    if (取消收藏?) then (yes)
      |User|
      :删除收藏记录;
      |Frontend|
      :实时更新列表;
    endif
    if (查看详情?) then (yes)
      |Audio|
      :获取音频详情;
     |Frontend|
      :显示音频详情;
    endif
  else (音频失效)
    |Frontend|
    :标记为“失效音频”，允许用户移除;
  endif
else (获取失败)
  |Frontend|
  :提示“获取收藏列表失败”;
endif

stop
@enduml
