@startuml
|Frontend|
start
:点击“退出登录”;

:弹出确认对话框;

if (用户选择?) then (确认退出)
  |Auth|
  :向服务端发起退出请求;
  |Frontend|
  :清除客户端存储的 JWT Token 及用户状态;
  |Auth|
  if (网络错误?) then (yes)
    :仍清除本地数据;
    |Frontend|
    :显示“已退出登录”;
  else (no)
     |Frontend|
    :显示“已退出登录”;
  endif
  |Frontend|
  :跳转回未登录状态的主页;
else (取消)
  :关闭对话框，保持登录状态;
endif

stop
@enduml