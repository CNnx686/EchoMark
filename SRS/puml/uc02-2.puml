@startuml
|Frontend|
start
:用户点击"忘记密码";
:跳转至密码重置页面;
:用户输入邮箱/手机号申请重置;
|Auth|
:验证邮箱/手机号是否注册;
if (账号存在?) then (yes)
  :生成一次性验证码或重置链接;
  :通过邮件/SMS发送给用户;
  
  |Frontend|
  :用户输入验证码或点击重置链接;
  |Auth|
  :验证验证码/链接有效性;
  if (验证成功?) then (yes)
    |Frontend|
    :允许用户设置新密码;
    |Auth|
    :更新用户密码（加密存储）;
    if (更新成功?) then (yes)
      |Frontend|
      :提示“密码已重置，请重新登录”;
    else (失败)
      :提示“密码重置失败，请重试”;
    endif
  else (验证失败)
    :提示“验证码无效或已过期”;
  endif
else (账号不存在)
  :提示“该邮箱/手机号未注册”;
endif

stop
@enduml