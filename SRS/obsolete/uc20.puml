@startuml
|OtherUser|
start
:对用户的声音记录进行点赞或评论;

|System|
:生成互动通知（含声音ID、互动类型、互动者信息）;
:显示通知红点;

|User|
:点击通知图标;
:进入通知中心;

:加载互动通知列表\n（包括点赞/评论、互动者、时间等）;
if (加载成功?) then (no)
  |User|
  :提示“通知加载失败，请重试”;
  stop
endif

|User|
:点击某条互动通知;

|System|
:检查关联的声音记录是否存在;
if (声音记录存在?) then (no)
  |User|
  :提示“该语音记录已不存在”;
  stop
endif
|System|
:检查互动用户状态;
if (互动用户已注销?) then (yes)
  |User|
  :显示发送者为“已注销用户”;
endif

:尝试跳转至对应声音记录播放页面\n并高亮相关评论/点赞;
if (跳转成功?) then (no)
  |User|
  :提示“跳转失败”;
  :停留在通知列表页面;
  stop
endif

:将该通知标记为已读状态;

|User|
if (选择“通知静默”?) then (yes)
  |System|
  :关闭未读互动通知红点提示;
  :后续不再显示红点;
endif

stop
@enduml