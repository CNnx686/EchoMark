@startuml
|User|
start
:进入“个人中心”页面;
:点击“注销账号”按钮;

:弹出注销警告页，要求输入\n确认文字“我确认注销账号{用户名}”\n及当前登录密码;

|User|
if (用户选择?) then (取消)
  :关闭警告页，保持账号状态;
  stop
else (继续)
  :输入确认文字和密码;
  :点击最终确认按钮;
endif

if (确认文字正确?) then (no)
  |User|
  :提示“确认文字不匹配”;
  stop
endif

:验证密码是否正确;
if (密码正确?) then (no)
  |User|
  :提示“密码错误”;
  stop
endif

|User|
:执行账号注销逻辑\n（标记为已删除，异步清理关联数据）;
|User|
if (注销请求成功?) then (no)
  |User|
  :提示“注销请求提交失败，请重试”;
  stop
endif

:清除客户端登录状态（JWT Token等）;
:提示“账号{用户名}已注销”;
:跳转至首页;

|User|
stop
@enduml